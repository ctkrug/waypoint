"""``python -m waypoint`` — inspect and manage stored checkpoints.

Full ``status``/``clear`` subcommands land in Epic 2 (docs/BACKLOG.md).
"""

import sys


def main() -> int:
    print("waypoint CLI is not implemented yet - see docs/BACKLOG.md, Epic 2.")
    return 0


if __name__ == "__main__":
    sys.exit(main())
